https://chatgpt.com/g/g-p-6910b5d314688191a14f8639f3a23814-home-automation/project

https://youtu.be/AitCKcyjHuQ

#define BLYNK_TEMPLATE_ID "8072YC"
#define BLYNK_TEMPLATE_NAME "Home Automation"
#define BLYNK_AUTH_TOKEN "xvlAklHCFcnQkwsNZmT"

#include <WiFi.h>
#include <WiFiClient.h>
#include <BlynkSimpleEsp32.h>
#include "DHT.h"

char auth[] = BLYNK_AUTH_TOKEN;
char ssid[] = "Your_WiFi_Name";
char pass[] = "Your_WiFi_Password";

#define DHTPIN 4
#define DHTTYPE DHT11

#define RELAY1 23
#define RELAY2 22
#define RELAY3 21
#define RELAY4 19

DHT dht(DHTPIN, DHTTYPE);
BlynkTimer timer;

// Read DHT11
void sendSensor() {
  float h = dht.readHumidity();
  float t = dht.readTemperature();

  if (isnan(h) || isnan(t)) return;

  Blynk.virtualWrite(V5, t); // Temperature
  Blynk.virtualWrite(V6, h); // Humidity
}

// Relay control from app
BLYNK_WRITE(V1) { digitalWrite(RELAY1, param.asInt()); }
BLYNK_WRITE(V2) { digitalWrite(RELAY2, param.asInt()); }
BLYNK_WRITE(V3) { digitalWrite(RELAY3, param.asInt()); }
BLYNK_WRITE(V4) { digitalWrite(RELAY4, param.asInt()); }

void setup() {
  Serial.begin(115200);
  Blynk.begin(auth, ssid, pass);
  dht.begin();

  pinMode(RELAY1, OUTPUT);
  pinMode(RELAY2, OUTPUT);
  pinMode(RELAY3, OUTPUT);
  pinMode(RELAY4, OUTPUT);

  digitalWrite(RELAY1, HIGH);
  digitalWrite(RELAY2, HIGH);
  digitalWrite(RELAY3, HIGH);
  digitalWrite(RELAY4, HIGH);

  timer.setInterval(2000L, sendSensor);
}

void loop() {
  Blynk.run();
  timer.run();
}

https://youtu.be/zYZyWLTe_MQ?si=LdxrSgL847YkAuTC
https://youtu.be/zYZyWLTe_MQ?si=C0pneqS6ywsueTDI